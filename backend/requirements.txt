# ------------------------------------------------------------------
# MINIMAL REQUIREMENTS FOR VERCEL PYTHON SERVERLESS DEPLOY
#
# On Vercel the serverless function has a 250MB size limit (unzipped).
# The full requirements.txt used on Emergent is too heavy (pandas,
# numpy, boto3, tooling). This file contains only what server.py needs
# at runtime.
#
# HOW TO USE (before the FIRST Vercel deploy):
#   cd backend
#   mv requirements.txt requirements-dev.txt
#   mv requirements-vercel.txt requirements.txt
#   git add -A && git commit -m "Use minimal deps for Vercel" && git push
#
# After this, Vercel will install only these packages at build time.
# ------------------------------------------------------------------

#fastapi==0.110.1
#python-dotenv>=1.0.1
#motor==3.3.1
#pydantic>=2.6.4
#email-validator>=2.2.0
#starlette>=0.36.0

# ------------------------------------------------------------------
# MINIMAL REQUIREMENTS FOR VERCEL PYTHON SERVERLESS DEPLOY
# ------------------------------------------------------------------
fastapi==0.110.1
python-dotenv==1.0.1
motor==3.3.2
pymongo==4.6.1
mangum==0.17.0
pydantic==2.6.4
email-validator==2.2.0
starlette==0.37.2
httpx
sumup
